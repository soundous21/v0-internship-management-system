
{% extends 'base.html.twig' %}

{% block title %}Home - Events Medical{% endblock %}

{% block body %}
    <div class="container text-center py-5">
        <h1 class="animated-title mb-4">{{ message }}</h1>
        <p class="text-white">This is the main page of your Events Medical platform.</p>

        <div class="d-flex justify-content-center gap-3 mt-4">
            <a href="{{ path('super_dashboard') }}" class="btn btn-glow">Super Admin Dashboard</a>
            <a href="{{ path('organizer_dashboard') }}" class="btn btn-glow">Organizer Dashboard</a>
        </div>
    </div>
{% endblock %}
